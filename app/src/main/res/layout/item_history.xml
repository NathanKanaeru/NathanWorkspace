<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:contentPadding="16dp"
    app:strokeColor="?attr/colorOutline"
    app:strokeWidth="1dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Status icon circle background -->
        <View
            android:id="@+id/v_status_bg"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@drawable/bg_status_circle"
            android:backgroundTint="#2034A853"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:id="@+id/iv_icon"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:contentDescription="Status"
            app:layout_constraintBottom_toBottomOf="@id/v_status_bg"
            app:layout_constraintEnd_toEndOf="@id/v_status_bg"
            app:layout_constraintStart_toStartOf="@id/v_status_bg"
            app:layout_constraintTop_toTopOf="@id/v_status_bg" />

        <!-- Run ID -->
        <TextView
            android:id="@+id/tv_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:textAppearance="@style/TextAppearance.Material3.LabelLarge"
            android:textColor="?attr/colorOnSurface"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@id/tv_conclusion"
            app:layout_constraintStart_toEndOf="@id/v_status_bg"
            app:layout_constraintTop_toTopOf="@id/v_status_bg" />

        <!-- Conclusion chip -->
        <TextView
            android:id="@+id/tv_conclusion"
            android:layout_width="wrap_content"
            android:layout_height="24dp"
            android:gravity="center"
            android:paddingHorizontal="10dp"
            android:text="SUCCESS"
            android:textAppearance="@style/TextAppearance.Material3.LabelSmall"
            android:textColor="@color/white"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@id/btn_view"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Time -->
        <TextView
            android:id="@+id/tv_time"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="2dp"
            android:textAppearance="@style/TextAppearance.Material3.BodySmall"
            android:textColor="?attr/colorOnSurfaceVariant"
            app:layout_constraintEnd_toEndOf="@id/tv_title"
            app:layout_constraintStart_toEndOf="@id/v_status_bg"
            app:layout_constraintTop_toBottomOf="@id/tv_title" />

        <!-- View on GitHub button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_view"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:padding="0dp"
            app:icon="@drawable/ic_eye"
            app:iconSize="20dp"
            app:iconTint="@color/blue_primary"
            app:layout_constraintBottom_toBottomOf="@id/v_status_bg"
            app:layout_constraintEnd_toStartOf="@id/btn_rerun"
            app:layout_constraintTop_toTopOf="@id/v_status_bg" />

        <!-- Re-run button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_rerun"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:padding="0dp"
            app:icon="@drawable/ic_play"
            app:iconSize="20dp"
            app:iconTint="@color/blue_primary"
            app:layout_constraintBottom_toBottomOf="@id/v_status_bg"
            app:layout_constraintEnd_toStartOf="@id/btn_delete"
            app:layout_constraintTop_toTopOf="@id/v_status_bg" />

        <!-- Delete button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_delete"
            style="@style/Widget.Material3.Button.TextButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:padding="0dp"
            app:icon="@drawable/ic_delete"
            app:iconSize="20dp"
            app:iconTint="?attr/colorError"
            app:layout_constraintBottom_toBottomOf="@id/v_status_bg"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/v_status_bg" />

        <!-- Log preview (expandable) -->
        <TextView
            android:id="@+id/tv_log_preview"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:ellipsize="end"
            android:fontFamily="monospace"
            android:maxLines="2"
            android:textAppearance="@style/TextAppearance.Material3.BodySmall"
            android:textColor="@color/logs_text"
            android:visibility="gone"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/v_status_bg" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>